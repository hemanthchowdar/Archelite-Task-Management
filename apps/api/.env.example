# Required — your Postgres connection string
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/ctms_dev"

# Required — min 32 characters, used for signing JWTs
JWT_SECRET="change-this-to-a-real-secret-at-least-32-chars"

# Optional — defaults shown
PORT=3001
NODE_ENV=development
CORS_ORIGIN=*
